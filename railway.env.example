# Railway Environment Variables Template
# Copy this to your Railway service variables

# ============================================
# Backend API, Celery Worker, Celery Beat
# ============================================

# Database (auto-provided by Railway PostgreSQL service)
DATABASE_URL=${PGDATABASE_URL}

# Redis (auto-provided by Railway Redis service)
REDIS_URL=${REDIS_URL}
CELERY_BROKER_URL=${REDIS_URL}
CELERY_RESULT_BACKEND=${REDIS_URL}

# Logging
LOG_LEVEL=INFO

# ============================================
# Frontend Web
# ============================================

# Backend API URL (replace with your actual backend Railway URL)
EXPO_PUBLIC_API_BASE_URL=https://your-backend-api.railway.app

# ============================================
# Notes:
# ============================================
# 1. ${PGDATABASE_URL} and ${REDIS_URL} are automatically 
#    provided by Railway when you add PostgreSQL and Redis services
# 2. Replace 'your-backend-api.railway.app' with your actual backend URL
# 3. These variables should be added in Railway dashboard under 
#    Settings → Variables for each service

